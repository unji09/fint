package com.s14p31a301.fint.app.navigation

import androidx.compose.runtime.Composable

/**
 * 앱 NavHost.
 * 기본 시작점은 WebView(Web), 그리고 Bridge 호출에 의해 Native 화면으로 이동.
 */
@Composable
fun FintNavHost() {
    // TODO: NavHost(startDestination = AppRoute.Web.path) { ... }
}


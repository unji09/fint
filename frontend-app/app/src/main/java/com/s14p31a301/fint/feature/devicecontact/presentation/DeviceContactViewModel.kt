package com.s14p31a301.fint.feature.devicecontact.presentation

import androidx.lifecycle.ViewModel

class DeviceContactViewModel : ViewModel() {
    // TODO: load(), search(query), select(contact), register()
}


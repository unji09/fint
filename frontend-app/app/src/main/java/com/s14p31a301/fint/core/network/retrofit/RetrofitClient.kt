package com.s14p31a301.fint.core.network.retrofit

/**
 * Retrofit 인스턴스 빌더.
 * - BASE_URL, OkHttp(AuthInterceptor), JSON 컨버터 설정 담당.
 */
object RetrofitClient {
    // TODO: fun create(baseUrl: String, tokenProvider: () -> String?): Retrofit
}

